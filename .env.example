# OpenAI API key — https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: override the model used by all three assignments.
# Default is gpt-4.1-mini, chosen for cost; it supports tool calling and structured output.
# OPENAI_MODEL=gpt-4.1-mini
